{
  "name": "ion-viper",
  "version": "0.1.0",
  "description": "Ion Viper is a Raiden-style vertical shooter.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 8080",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "playwright test --config tests/playwright.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
